//
// Created by brout_m on 11/05/17.
//

#include "types.h"

int list_folder(t_work *work, const char *path)
{
  (void)path;
  (void)work;
  return (0);
}

int list_file(t_work *work, const char *path)
{
  (void)path;
  (void)work;
  return (0);
}

int exec_list_command(t_work *work, char *command)
{
  (void)command;
  (void)work;
  return (0);
}
